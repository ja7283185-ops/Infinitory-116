package furgl.infinitory.network;

import net.minecraft.util.Identifier;

public class InfinitoryNetworking {

	/** C2S: sent when the player clicks the sort button in their inventory. No payload. */
	public static final Identifier CYCLE_SORT_PACKET_ID = new Identifier("infinitory", "cycle_sort");
}
